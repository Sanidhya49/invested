name: frontend_aditi
description: A new Flutter project
version: 1.0.0

environment:
  sdk: '^3.8.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.2
  firebase_auth: ^4.16.0
  google_sign_in: ^6.2.1
  http: ^1.2.1
  url_launcher: ^6.2.5
  # Add these two packages
  intl: ^0.19.0 # For currency formatting
  # lucide_flutter: ^1.408.0 # For icons that match your UI